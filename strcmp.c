#include<stdio.h>
int main()
{
	char str[50],str2[40];
	puts("Enter any string: ");
	gets(str);
	puts("Enter any string: ");
	gets
	(str2);
	if(strcmp(str,str2)==0)
		puts("Both string are equal");
	else
		puts("Both string are not equal");
	return 0;
}

