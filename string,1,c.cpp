#include<stdio.h>
#include<string.h>
int main()
{
	char str[50];
	puts("Enter Your Number: ");
	gets(str);
	
	puts("Enter Your Number: ");
	gets(str);
	strcat(str,str2)
	printf(puts(str));
	return 0;
	
}
